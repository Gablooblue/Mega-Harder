<%= form_with(model: transaction_item, local: true) do |form| %>
  <% if transaction_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transaction_item.errors.count, "error") %> prohibited this transaction_item from being saved:</h2>

      <ul>
      <% transaction_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :medicine_type_id %>
    <%= form.number_field :medicine_type_id, id: :transaction_item_medicine_type_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :transaction_item_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
